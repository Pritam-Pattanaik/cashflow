# Fix Plan: Serverless Functions for Vercel (No WebSocket)

## Status: ✅ IMPLEMENTED

All changes have been applied successfully. Client build passed.

## Changes Made

### 1. Client Build Fixes (to fix WebSocket/HMR errors)
- **`client/vite.config.ts`** - Standard React plugin with HMR overlay disabled
- **`client/tsconfig.app.json`** - Removed `erasableSyntaxOnly` for Node 20 compatibility
- **`client/tsconfig.node.json`** - Simplified config, removed problematic settings
- **`client/package.json`** - Standard dependencies with `@vitejs/plugin-react@5.0.0`
- **`client/eslint.config.js`** - Restored react-refresh for dev linting

### 2. Serverless Function (`api/index.ts`)
- Added server caching to prevent re-initialization on each request
- Proper Express adapter setup for Vercel Node.js runtime

### 3. Vercel Configuration (`vercel.json`)
- Uses `builds` array with `@vercel/node` for API and `@vercel/static-build` for client
- Proper routing for `/api/*` to serverless function and `/*` to static files

### 4. Root package.json
- Added `engines.node: 20.x` to match Vercel's Node version
- Updated `vercel-build` script for proper monorepo build order

## Deployment Steps for Vercel

1. **Set Environment Variables** in Vercel:
   - `DATABASE_URL` - Your PostgreSQL connection string
   - `JWT_SECRET` - A secure random string for JWT signing

2. **The project structure now supports:**
   - Serverless API at `/api/index.ts` (NestJS)
   - Static client at `/client/dist/`

## Local Development

To run locally:
```bash
# Terminal 1 - Server
cd server
npm run start:dev

# Terminal 2 - Client  
cd client
npm run dev
```

## Test Login Credentials
- Email: `owner@cashflow.com`
- Password: `password123`